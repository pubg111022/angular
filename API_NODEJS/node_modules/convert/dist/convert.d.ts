import { Converter } from './types/units';
declare type OverloadedConverter = ((quantity: number) => Converter<number>) & ((quantity: bigint) => Converter<bigint>);
export declare const enum UnitIndexes {
    Family = 0,
    Ratio = 1,
    Difference = 2
}
/**
 * Convert a quantity from one unit to another.
 *
 * @example
 * ```ts
 * convert(90).from('minutes').to('h'); // 1
 * ```
 * @example
 * ```ts
 * convert(2n).from('hours').to('min'); // 120n
 * ```
 *
 * @param quantity - The quantity you want to convert
 */
export declare const convert: OverloadedConverter;
export {};
