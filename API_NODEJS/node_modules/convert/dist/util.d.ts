/**
 * Throws an errror if `condition` is falsy.
 * @private
 * @param condition The condition to check
 * @param message The error message to display in development builds
 */
export declare function invariant(condition: unknown, message: string): asserts condition;
/**
 * Each unit has an element that identifies what family it belongs to (ex. length or time).
 * You can use this to group units together when display them (ex. a dropdown GUI).
 */
export declare const enum UnitFamilies {
    Data = 0,
    Length = 1,
    Mass = 2,
    Pressure = 3,
    Temperature = 4,
    Time = 5,
    Volume = 6,
    Angle = 7,
    Force = 8
}
export declare const unitFamilies: {
    readonly Data: UnitFamilies.Data;
    readonly Length: UnitFamilies.Length;
    readonly Mass: UnitFamilies.Mass;
    readonly Pressure: UnitFamilies.Pressure;
    readonly Temperature: UnitFamilies.Temperature;
    readonly Time: UnitFamilies.Time;
    readonly Volume: UnitFamilies.Volume;
    readonly Angle: UnitFamilies.Angle;
    readonly Force: UnitFamilies.Force;
};
